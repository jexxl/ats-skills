---
课题名称: 自主式交通系统跨域计算与决策优化
专题名称: 多方式异智系统自主闭环跨域管控决策计算及算法
模型编号: { 算法编号 }
模型名称: { 算法名称 }
模型功能描述: { 模型功能描述 }
输入数据概括: { 一句话输入数据概括 }
输出数据概括: { 一句话输出数据概括 }
模型服务场景: { 城市路网群智协同信号控制 / 城市路网异智车辆路径规划与信号控制跨域协同决策 / 城市通勤走廊多模式交通自主协同运行 / 面向轨道激增客流应急疏散的多方式自主协同管控与服务 }
上游接口模型编号: "-"
下游接口模型编号: "-"
交付时间: { 当前日期 }
责任单位: 同济大学
---

# {算法编号} {算法名称}

## 算法模型基本信息

### 输入数据要求

> 必须按以下格式填写：每个数据块先写一行“数据名称”，下一行写“(`文件名`)”，再从下一行开始写三列表格；表头必须为 `字段中文`、`字段英文`、`字段类型`，不得改名、合并列或写成列表。

{输入数据名}
(`{输入文件名}`)

| 字段中文 | 字段英文 | 字段类型 |
| --- | --- | --- |
| {字段中文1} | `{字段英文1}` | `int/float/str` |
| {字段中文2} | `{字段英文2}` | `int/float/str` |

### 输出数据要求

> 必须按以下格式填写：每个数据块先写一行“数据名称”，下一行写“(`文件名`)”，再从下一行开始写三列表格；表头必须为 `字段中文`、`字段英文`、`字段类型`，不得改名、合并列或写成列表。

{输出数据名}
(`{输出文件名}`)

| 字段中文 | 字段英文 | 字段类型 |
| --- | --- | --- |
| {字段中文1} | `{字段英文1}` | `int/float/str` |
| {字段中文2} | `{字段英文2}` | `int/float/str` |

## 算法模型简介

{算法模型文字简介。应聚焦算法原理，不写代码、工程文件、运行命令或实现细节；可附带必要公式并解释变量含义。}

公式规范：行内公式使用 `$...$` 或 `\(...\)`；独立公式使用 `$$...$$` 或 `\[...\]`。只使用上下标、分式、根号、求和、积分、极值、希腊字母、比较符、集合符号和 `\operatorname{...}`、`\mathrm{...}`、`\text{...}` 等常见命令。禁止使用自定义宏、公式编号/引用，以及 `equation`、`align`、`aligned`、`gather`、`multline`、`array`、`matrix` 等 LaTeX 环境；多行推导拆成多个独立公式块。

## 算法模型流程图

```plantuml
@startuml
start
:读取输入数据;
:执行核心算法;
:输出结果;
stop
@enduml
```

## 测试数据与配置

本节记录本次测试实际采用的输入数据、配置参数和数据样例，用于说明模型测试所依据的原始实验数据。

### 测试输入文件清单

| 文件名 | 格式 | 是否必须 | 说明 |
| ------ | ---- | -------- | ---- |
| `{文件名1}` | `csv/json/toml/xlsx/npz/parquet/xml` | 是 | {简要说明} |
| `{文件名2}` | `csv/json/toml/xlsx/npz/parquet/xml` | 是 | {简要说明} |

### 实际输入数据

> 按实际文件类型展开：表格型数据写真实行节选；JSON/TOML/XML 写关键片段；NPZ 写数组名、shape、dtype、索引范围、对象顺序和关键数值或矩阵切片；二进制权重写文件作用、大小和可验证摘要。

#### {输入数据名称1}（节选）

(`{输入文件名1}`)

{用一句话说明该输入数据的真实规模、覆盖对象和用途，例如“该数据记录……，共 N 条记录，涵盖 M 个对象”。}

| {字段中文1} | {字段中文2} | {字段中文3} |
| ----------- | ----------- | ----------- |
| {真实值1} | {真实值2} | {真实值3} |
| {真实值4} | {真实值5} | {真实值6} |

#### {输入数据名称2}

(`{输入文件名2}`)

{用一句话说明该输入文件定义的对象、参数或配置含义。JSON/TOML/XML 等非表格数据保留关键片段即可。}

```json
{
  "{真实键}": "{真实值}"
}
```

### 可泛化参数

在不修改算法核心逻辑的前提下，可通过调整以下参数改变算法行为或适配不同场景。

**`data/input/{配置文件名}`**

{对该配置文件的整体用途作一句话描述。}

| 参数名 | 类型 | 默认值 | 取值范围 | 含义与影响 |
| ------ | ---- | ------ | -------- | ---------- |
| `{参数1}` | `int/float/str/bool` | `{默认值}` | `{取值范围}` | {参数含义及修改后的影响} |

## 测试过程

本次测试通过容器方式复现算法运行过程，将输入数据目录和输出数据目录挂载到容器内，运行结束后检查输出文件及关键结果。

### 运行方式

```bash
# 加载镜像
docker load -i {打包编号}.tar

# Linux/macOS
docker run --rm \
  -v $(pwd)/data/input:/app/data/input \
  -v $(pwd)/data/output:/app/data/output \
  {打包编号}:v1

# Windows PowerShell
docker run --rm `
  -v "${PWD}/data/input:/app/data/input" `
  -v "${PWD}/data/output:/app/data/output" `
  {打包编号}:v1
```

将所需输入文件放入 `data/input/` 目录，运行结束后结果文件生成于 `data/output/`。

### 输出文件清单

| 文件名 | 格式 | 是否必须 | 说明 |
| ------ | ---- | -------- | ---- |
| `{文件名1}` | `csv/json/toml/xlsx/npz/parquet/xml` | 是 | {简要说明} |

## 测试结果

> 按实际输出类型展开：表格型输出写真实表格；JSON/XML 写关键字段片段；NPZ 写数组名、shape、dtype、索引范围、对象顺序和代表性矩阵或关键数值。不得只写“预期输出”或文件清单。

### {输出数据名称1}

(`{输出文件名1}`)

{用一句话说明该输出结果的含义和计算来源。}

| {字段中文1} | {字段中文2} | {字段中文3} |
| ----------- | ----------- | ----------- |
| {真实值1} | {真实值2} | {真实值3} |
| {真实值4} | {真实值5} | {真实值6} |

### {输出数据名称2}（节选）

(`{输出文件名2}`)

{对于 `npz` 等二进制输出，必须写明数组名称、shape、索引范围、对象顺序和代表性矩阵或关键数值。}

{数组或结果摘要，例如：`{数组名}` 形状为 `{shape}`，对象顺序为 `[{对象1}, {对象2}]`。以第 `{k}` 个时步为例：}

|  | {对象1} | {对象2} |
| - | ------ | ------ |
| {对象1} | {真实值} | {真实值} |
| {对象2} | {真实值} | {真实值} |

## 测试结论

本次测试使用 `{输入文件1}`、`{输入文件2}` 和 `{配置文件}` 作为输入，在容器环境中按报告记录的运行方式完成执行，运行过程未出现错误。

测试完成后，模型生成 `{输出文件1}`、`{输出文件2}` 等输出文件。经核验，关键结果 `{关键结果名称}` 的 `{shape/字段/统计值}` 为 `{真实结果摘要}`，输出文件格式、字段名称、数组维度和关键数值与“输出数据要求”及本报告“测试结果”记录一致。

综上，{算法名称}（{算法编号}）在本次测试数据和配置条件下通过运行测试，能够满足所定义的输入输出接口和功能要求。
